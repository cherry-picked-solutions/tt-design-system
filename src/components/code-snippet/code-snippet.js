import { LitElement, html } from "lit";

/**
 * @tag tt-code-snippet
 * @summary A description.
 *
 * @cssprop [--tt-example=var(--token)]
 *
 */

export class TTCodeSnippet extends LitElement {
  static properties = {};

  constructor() {
    super();
  }

  render() {
    return html``;
  }
}

customElements.define("tt-code-snippet", TTCodeSnippet);
